const caesarCipher = require("../katas/caesar-cipher")

// should return "" if there is nothing passed in as a string
// it should return the untouched string if the number == 0
// 



describe('caesarCipher()', () => {
    it('should ', () => {
        
    });
});